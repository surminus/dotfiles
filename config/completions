echo -n $'############  (88%)\r'

for i in helm awless; do
  test -f /usr/local/bin/$i && \
  source <($i completion zsh)
done

echo -n $'############  (93%)\r'
test -f ~/surminus/to-do/to-do.completion && source ~/surminus/to-do/to-do.completion
